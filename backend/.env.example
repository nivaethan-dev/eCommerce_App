# Copy this file to  and fill in the values

# Server
HOST=127.0.0.1
PORT=3000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/ecommerce_dev

# Auth (use strong random strings in real .env)
ADMIN_JWT_SECRET=replace-with-strong-admin-secret
ADMIN_REFRESH_JWT_SECRET=replace-with-strong-refresh-secret
CUSTOMER_JWT_SECRET=replace-with-strong-customer-secret
CUSTOMER_REFRESH_JWT_SECRET=replace-with-strong-refresh-secret
